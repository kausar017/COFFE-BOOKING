
import PropTypes from 'prop-types';

const Coffees = props => {
    return (
        <div>
            <h1>Coffes</h1>
        </div>
    );
};

Coffees.propTypes = {
    
};

export default Coffees;