
import PropTypes from 'prop-types';

const Dashbord = props => {
    return (
        <div>
            <h1>Dash bord</h1>
        </div>
    );
};

Dashbord.propTypes = {
    
};

export default Dashbord;